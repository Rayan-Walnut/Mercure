{
  "name": "mercure",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "dev": "tsc --watch & electron ."
  },
  "devDependencies": {
    "electron": "^29.0.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}